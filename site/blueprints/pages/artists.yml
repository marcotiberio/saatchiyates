title: Artists

options:
  changeStatus: false

columns:
  main:
    width: 2/3
    fields:
      # files:   
      #   type: files
      #   label: Artists Cover Picture (do not use)
      #   template: image

      coverImage:
        label: Cover Images
        type: files

      seometa:
        label: Meta Description
        type: textarea
        size: small
        width: 1/2
        
      seotags:
        label: Meta Keywords
        type: textarea
        size: small
        width: 1/2

  # sidebar
  sidebar:
    width: 1/3
    sections:
      extends: sections/artist
